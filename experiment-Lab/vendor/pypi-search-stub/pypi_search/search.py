# Stub module: the real 'pypi-search' package was deleted from PyPI.
# This project's experiment code never imports it - only psychopy's
# optional GUI plugin manager (psychopy.app.plugin_manager) does.
